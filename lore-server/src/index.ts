// The MCP server is now integrated into the API.
// Run: bun run start:api (or just bun run start)
// MCP endpoint: http://localhost:3001/mcp

console.log("MCP is now integrated into the API server.");
console.log("Run: bun run start:api");
console.log("MCP endpoint: http://localhost:3001/mcp");
process.exit(0);
